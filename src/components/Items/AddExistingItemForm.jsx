import axios from "axios";
import API_URL from "../../services/apiConfig";

const AddExistingItemForm = ({ selectedOption }) => {
  console.log(selectedOption);

  return <div></div>;
};

export default AddExistingItemForm;
